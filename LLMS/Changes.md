Read `AGENTS.md` at repo root, then `docs/agents/INDEX.md`.

File purpose: Track feature requests, bugs, refactors, and investigations that should later be implemented or carried out here.

Guidelines:
- One item per entry with a clear title and short description.
- Include priority and impact if known (e.g., High/Med/Low).
- Mark status explicitly (e.g., `todo`, `done`).
- Keep this list up to date; remove or archive items that are resolved.
- When an item references a design in `LLMS/Design`, keep the design link current.
- Before implementing an item that has a design, review that design for unresolved review points, open questions, risks, follow-ups, or proposed defaults that still need user confirmation.
- If those unresolved points materially affect scope, behavior, data shape, UX, or rollout, ask the user to answer or approve them before implementation.
- When implementing an item from this file, update all relevant project documentation in `docs/agents` as part of the same change.
- If documentation is added, moved, or removed during implementation, update `docs/agents/INDEX.md` too.

# Changes Queue
- [done] [23/04/2026] Run second CMS UI primitive sweep for leftover bespoke modal/menu surfaces after shadcn migration. Priority: Med. Impact: dashboard page delete flow, block-list item actions, and sidebar content action menus still used hand-built overlays or direct Bits UI menus, which kept styling drift and duplicated interaction patterns after wider shadcn adoption. Resolution: moved dashboard delete confirmation onto shared `ActionModal`, switched block-list row actions to repo dropdown-menu primitives with submenu-based reusable insertion, and replaced remaining sidebar action menus with repo dropdown-menu wrappers/buttons for tighter CMS consistency.
- [done] [23/04/2026] Tighten Codex guidance layering around smaller always-on policy, narrower local rules, invariant-first reference docs, and workflow-only skills. Priority: High. Impact: current guidance still carried broad `src/lib` instructions, duplicated UI/editor rules across docs and skills, and kept some volatile behavior notes in agent-facing layers that were larger than needed for Codex retrieval. Resolution: shrank root `AGENTS.md`, replaced `src/lib/AGENTS.md` with narrower `src/lib/server/AGENTS.md` and `src/lib/components/cms/AGENTS.md`, rebuilt `docs/agents/INDEX.md` as routing hub, rewrote high-value workflow docs around invariants, retained only procedural skills, and removed `cms-page-editor` skill because it was descriptive workflow reference rather than reusable operating playbook.
- [done] [22/04/2026] Reorganize repository agent-facing docs into Codex-native layered guidance system. Priority: High. Impact: root instructions were overloaded, heavy guidance sat in always-read paths, and documentation lived in one LLMS-specific tree with duplicate workflow prose instead of Codex-native routing, local instructions, and reusable skills. Resolution: rewrote root/local `AGENTS.md` files, moved canonical agent guidance into `docs/agents/`, added repo-local workflow skills under `.agents/skills/`, kept `LLMS/Changes.md`/`LLMS/Suggestions.md`/`LLMS/Design/` as queue and reference system, updated README, and removed redundant legacy guidance files from `LLMS/Documentation`.
- [done] [22/04/2026] Replace hand-crafted CMS controls with shadcn-svelte primitives where possible and wire dark/light mode across app shell. Priority: High. Impact: core routes had drifted into bespoke buttons/forms/dialogs with no shared component baseline and light-only color stacks, so UI work duplicated effort and dark mode effectively did not exist. Resolution: installed repo-local shadcn-svelte primitives plus `mode-watcher`, added shared theme toggle + layout watcher, moved dashboard/auth/content/trash/editor flows and shared modal/icon controls onto shadcn buttons/inputs/textareas/alerts/badges/tabs/alert-dialog where practical, updated CMS UI docs, and verified typecheck + unit tests.
- [done] [22/04/2026] Rewrite shared CMS toast renderer to match slab-style reference toasts. Priority: High. Impact: previous toast rewrite drifted from reference with over-rounded cards, wrong spacing/chrome, and weak progress treatment, so action feedback still felt visibly wrong. Resolution: replaced toast viewport markup/styles with new slab renderer, tightened tone styling around icon/action/progress system, kept full-width muted progress track plus active fill, preserved action support, and updated toast documentation.
- [done] [21/04/2026] Redesign shared CMS toast visuals to match reference-style slab toasts with timed progress rail and configurable actions. Priority: Med. Impact: first-pass toast manager landed with generic success/error cards, no structured action model, and motion/styling that missed intended visual identity, so CMS feedback felt off-brand and could not support multi-action toasts. Resolution: expanded toast state to support tone-specific title/body/action payloads, rebuilt viewport styling around reference-inspired slab toasts with bottom progress fill tied to auto-dismiss, added persistent/actionable toast support, and documented the shared toast API.
- [done] [21/04/2026] Add shared CMS toast manager for action feedback. Priority: Med. Impact: page publish/save, content actions, trash restore, and similar CMS flows still inline success/error boxes inside each screen, which repeats feedback logic, bloats local route state, and scatters action results away from where editors expect transient confirmations. Design: `LLMS/Design/20260421-cms-toast-manager.md`. Resolution: added context-scoped CMS toast provider/render stack, migrated page editor, content editor, content library, and trash restore flows off inline action banners, kept blocking validation inline where editors still need field context, and added toast state plus route-level regression coverage.
- [done] [18/04/2026] Fix page draft save from Content and Discovery & Sharing tabs dropping identity fields. Priority: High. Impact: saving a page while `Identity` tab was inactive submitted no `title`, `parentPageId`, or `urlName`, so server validation failed with `Title is required` and blocked content/SEO edits. Resolution: promoted bug from `LLMS/Suggestions.md`, made page save action fall back to current draft/page values for fields from inactive tabs, so content and SEO saves now preserve identity state instead of failing validation.
- [done] [18/04/2026] Align content labels exactly with page labels without changing page flow. Priority: Low. Impact: content blocks had drifted from page wording, which made identical draft/publish states feel like different workflows. Resolution: reverted content publish-state wording back to page-matching labels so content now uses same `Unpublished`, `Published`, `Draft changes`, and `Saved draft changes` terminology as pages while leaving page behavior untouched.
- [done] [18/04/2026] Fix content editor action rail not submitting draft/publish form. Priority: High. Impact: content blocks could be created, but `/content/[id]` save/publish buttons lived outside the form while page-editor buttons lived inside it, so clicking the content rail primary action did nothing. Resolution: promoted bug from user report, moved content editor grid under one form, linked right-rail primary action back to that form, and bound name/folder fields to local state so dirty-state detection and revert behavior now match page editor flow.
- [done] [18/04/2026] Fix content-block save/publish actions depending on stale Supabase RPC implementations. Priority: High. Impact: content editor draft save and publish could fail when local database functions lagged behind app behavior, which blocked core `/content/[id]` editing even though UI and server routes were wired. Resolution: promoted bug from user report, moved reusable-content create/save/publish writes to server-side Supabase table operations so content editing no longer depends on older `create_reusable_block(...)`, `save_reusable_block_draft(...)`, or `publish_reusable_block(...)` function definitions being current.
- [done] [18/04/2026] Fix reusable content creation failing on `reusable_blocks.content` null constraint. Priority: High. Impact: creating shared content from `/content` crashed with Postgres `23502` because `create_reusable_block(...)` stopped writing the legacy `reusable_blocks.content` column even though schema still requires it. Resolution: promoted bug from user report, updated SQL function to seed both top-level `reusable_blocks.content` and initial draft version content from same payload.
- [done] [17/04/2026] Fix reusable content publish leaving draft state stuck dirty. Priority: High. Impact: publishing `/content/[id]` created a live version but left the old draft pointer in place, so content still looked like it had unpublished changes immediately after publish and editors could not reach a clean published state. Resolution: promoted bug from `LLMS/Suggestions.md`, updated `publish_reusable_block(...)` to archive current draft, create new published version, then create fresh clean draft clone, and added migration cleanup for already-affected content rows whose draft/published payloads match.
- [done] [17/04/2026] Align content editor draft/publish controls and sidebar state labels with page editor behavior. Priority: Low. Impact: `/content/[id]` still used older grouped status chips and content tree badges collapsed multiple states into generic `Draft`, so content editing felt inconsistent with page editor and content status was harder to scan. Resolution: promoted from `LLMS/Suggestions.md`, rebuilt content side rail around separate draft/publish panels matching page editor labels, added shared reusable-content publish-state helpers, and updated content/sidebar badges plus CMS docs to show `Unpublished`, `Published`, or `Draft changes`.
- [done] [17/04/2026] Remove unclear `Featured` toggle from content editor hero blocks. Priority: Low. Impact: hero content type exposed a `Featured` boolean in `/content/[id]`, but no CMS flow or frontend behavior used it, so editors saw unexplained toggle that added noise and implied missing functionality. Resolution: promoted from `LLMS/Suggestions.md`, removed `hero.featured` from block registry/default content shape, updated related tests, and refreshed CMS content-model documentation.
- [done] [17/04/2026] Hide idle page/content editor actions instead of showing disabled buttons. Priority: Low. Impact: page and content editors left `All changes saved` and inactive revert controls visible even when they had no purpose, which made draft rails look heavier and less state-aware. Resolution: promoted suggestion from `LLMS/Suggestions.md`, hid no-op primary/revert actions on page and content editors, added enter/exit motion for action visibility changes, and updated CMS docs.
- [done] [17/04/2026] Replace generic reset-draft editor action with state-aware revert label. Priority: Low. Impact: page editor used vague `Reset draft` wording and content editor had no matching revert action, so editors could not tell whether unsaved changes would return to last saved draft or currently published content. Resolution: promoted suggestion from `LLMS/Suggestions.md`, changed page/content editor secondary actions to `Revert changes to draft` or `Revert changes to published` based on currently loaded source state, restored content-editor revert behavior, and updated CMS docs.
- [todo] [17/04/2026] Add trash and restore workflow for deleted pages/content. Priority: High. Impact: current CMS delete actions hard-delete pages and content, so accidental deletion loses editor work immediately and gives no recovery path; content deletion also needs clearer handling when item is still used by pages or other content. Design: `LLMS/Design/20260417-trash-and-restore-workflow.md`.
- [done] [17/04/2026] Replace manual full-path page slugs with parent-based page URL model. Priority: High. Impact: page creation/editing currently requires editors to type full path strings like `/about/us/open-position`, which makes hierarchy edits error-prone, leaks URL structure into every form, and blocks cleaner parent/child page management. Design: `LLMS/Design/20260417-page-parent-based-url-model.md`. Resolution: moved page identity into `page_versions`, replaced slug editing with parent-page plus optional URL-name inputs, switched CMS page editing to stable `/edit/page/[id]` routes, computed draft/live paths from explicit parent links, allowed child publish against live parent graph, updated page migration/seed flow, and refreshed CMS docs for dashboard/sidebar/editor behavior.
- [done] [17/04/2026] Merge page/content editor draft actions into one primary button with state-driven label. Priority: Med. Impact: separate save/publish buttons plus standalone validation status make draft workflow heavier than needed, while editors mainly need one next-step action that clearly reflects whether work is saved, needs saving, can publish, or is blocked by validation. Resolution: replaced page and content editor dual-action controls with one state-driven primary button, added unsaved-change detection to content editor so publish appears only after draft save, removed standalone validation status row, and updated CMS docs for new action states.
- [done] [17/04/2026] Add page publish workflow to CMS page editor and page listings. Priority: High. Impact: pages already store draft and published version pointers in data layer, but CMS still behaves like draft-only editor, so editors cannot intentionally publish page changes or tell whether changes are live versus draft. Design: `LLMS/Design/20260417-page-publish-workflow.md`. Resolution: enriched page records with explicit publish-state metadata, added page-editor publish action plus clean post-publish draft regeneration, surfaced publish-state badges and live timestamps in dashboard/sidebar page lists, and updated CMS docs for page publish behavior.
- [done] [17/04/2026] Refresh CMS dashboard and content editor to match rebuilt page-editor quality. Priority: High. Impact: dashboard and `/content/[id]` still looked like older scaffold-era CMS screens, which made the product feel visually inconsistent after the edit-page overhaul and added unnecessary visual weight to daily editing work. Design: `LLMS/Design/20260417-dashboard-and-content-editor-refresh.md`. Resolution: rebuilt dashboard around calmer page-first workspace framing, flattened page rows, moved create-page flow into compact side module, demoted Content library to secondary utility panel, refactored content editor into main editing column plus persistent draft/publish rail, and updated CMS documentation to capture the shared editor-shell direction.
- [done] [17/04/2026] Remove top-level Content library picker from page edit screen. Priority: Low. Impact: page editor content area had redundant insertion UI because the same content could already be added from the sidebar or row-level insertion actions, which made the screen heavier without adding capability. Resolution: removed the top-level `Content library` dropdown from `BlockListEditor`, dropped its dead fallback prop path, and updated edit-page documentation to point editors at sidebar and row-level insertion paths only.
- [done] [17/04/2026] Introduce shared CMS icon system and replace selected high-frequency text actions with icons. Priority: Med. Impact: repeated text actions like sidebar quick-add consume too much horizontal space and visual attention, but ad-hoc inline SVG usage would make icon behavior, sizing, accessibility, and consistency harder to maintain. Resolution: added `@lucide/svelte`, centralized CMS icon imports through `src/lib/icons/index.ts`, introduced shared `CmsIconButton` for icon-only controls with required accessible labels, swapped sidebar content quick-add from text to plus icon, and replaced sidebar chevrons with Lucide icons for consistency.
- [done] [17/04/2026] Refine auth success motion so login rail exits as overlay instead of squeezing shell. Priority: Med. Impact: current success transition changes rail width, which makes editor teaser feel like layout resize instead of clean handoff into app. Resolution: extracted auth rail and preview stage into separate auth components, kept preview stage fixed beneath overlay rail, and changed success motion to slide rail left plus teaser right using transform/opacity instead of width animation.
- [done] [14/04/2026] Add Supabase auth gate and branded login/signup experience for CMS. Priority: High. Impact: CMS currently had no authentication, so all editor routes and actions were open by default; product also needed a deliberate signed-out entry flow before users reached editor. Design: `LLMS/Design/20260414-supabase-auth-and-login-experience.md`. Resolution: added Supabase SSR auth plumbing with request-scoped session validation, gated CMS routes/actions/API behind authenticated sessions, introduced `/auth` split login/signup screen with success transition toward editor shell, added sidebar signed-in state plus logout, documented auth setup/access flow, and added unit coverage for auth redirect helper.
- [done] [13/04/2026] Tighten confusing edit-page helper copy and remove redundant page-content header section. Priority: Med. Impact: the page editor showed unclear instructions in several states and repeated a `Page content` subheader that added noise without helping editors. Resolution: shortened page-level helper copy around page identity/content areas and removed the nested `Page content` title/description wrapper above the content list while keeping content-library insertion controls intact.
- [done] [13/04/2026] Remove remaining legacy top-level inline page blocks from the page editor after Content-library workflow shift. Priority: High. Impact: editors can still see old page-local blocks on edit pages, which conflicts with the new rule that page content should come only from the Content library. Resolution: deleted existing top-level inline page blocks directly from `public.page_versions.content` in local DB so only linked content refs remain, and tightened page-content validation so future top-level inline page blocks are rejected.
- [done] [13/04/2026] Shift CMS block workflow to a Content library model. Priority: High. Impact: editors should stop creating page-local inline blocks and instead manage all reusable content from one dedicated library, then place linked content into pages via drag/drop or library actions. Design: `LLMS/Design/20260413-content-library-workflow.md`. Resolution: added repo-level guidance to avoid backward-compat/migration work by default in early development, introduced `/content` routes and user-facing Content naming across dashboard/sidebar/editor surfaces, removed top-level inline block creation from the page editor, kept library-to-page insertion via drag/drop and actions, switched placed-content editing links to the dedicated content editor, and updated CMS documentation to match.
- [todo] [13/04/2026] Rebuild CMS edit page with cleaner, more coherent UI. Priority: High. Impact: current edit screen is functional but still reads like stacked admin scaffolding, which slows scanning and makes structure-heavy editing feel heavier than needed. Design: `LLMS/Design/20260413-edit-page-rebuild.md`. Notes: larger UI change; review and approve design before implementation.
- [done] [12/04/2026] Bug: CMS context menu seems not to be working. Priority: Med. Impact: editors may lose access to contextual actions in the CMS sidebar or related editing surfaces, which blocks important secondary workflows and makes existing affordances feel broken. Resolution: kept right-click handling in place and added visible `Actions` triggers for reusable-block folders and rows so the same operations remain available even when native context-menu behavior is unreliable.
- [done] [12/04/2026] Reduce page editor cognitive load and strengthen hierarchy. Priority: Low. Impact: makes the page editor easier to scan by surfacing draft/save state more clearly and reducing ambiguity around editing flow. Resolution: promoted from `LLMS/Suggestions.md` as a small change, added draft-status chips, reset-draft actions, earlier validation visibility, and clearer content guidance on the page editor.
- [done] [12/04/2026] Distill overloaded block-row controls in the page editor. Priority: Low. Impact: reduces per-row cognitive load so editors can focus on content instead of parsing every structural action at once. Resolution: promoted from `LLMS/Suggestions.md` as a small change, moved move-row controls behind a row-level `Actions` menu, reduced always-visible row metadata, and limited reorder drop targets to active drag states.
- [done] [12/04/2026] Make nested block editing feel less like stacked admin cards. Priority: Low. Impact: improves scanability of recursive block content by softening repeated nested editor chrome. Resolution: promoted from `LLMS/Suggestions.md` as a small change, lightened nested block containers and clarified nested-editor helper copy.
- [done] [12/04/2026] Improve page editor recovery and reassurance states. Priority: Low. Impact: gives editors clearer confidence about whether a draft is saved and how to abandon accidental edits. Resolution: promoted from `LLMS/Suggestions.md` as a small change, added saved/unsaved indicators, exposed validation state earlier, and added reset-to-last-save controls.
- [done] [12/04/2026] Rewrite page editor SEO helper copy for non-technical editors. Priority: Low. Impact: makes optional search and sharing settings easier for content managers to use without prior SEO knowledge. Resolution: promoted from `LLMS/Suggestions.md` as a small change, rewrote the SEO section labels and helper text in editor-facing language and updated the edit-page documentation.
- [done] [11/04/2026] Support dragging reusable blocks from the CMS sidebar into the page content editor. Priority: Med. Impact: shortens the path from finding shared content to inserting it into a page draft and makes reusable content feel connected to page editing instead of available only through a select picker. Design: `LLMS/Design/20260411-page-editor-sidebar-drag-drop.md`. Resolution: added a dedicated reusable-block insertion contract between the sidebar and page editor, enabled sidebar drag sources on wide pointer-capable edit layouts, added explicit between-row drop targets in the page editor, kept visible and context-menu insertion actions for touch/keyboard fallback, and documented the new sidebar/editor flow.
- [done] [11/04/2026] Reframe the CMS dashboard around a single primary page-management workflow instead of giving page management, reusable blocks, folder administration, and creation flows equal weight. Priority: High. Impact: makes the landing surface faster to scan, aligns the CMS with the stated fast/efficient/understandable product goal, and creates a clearer split between daily editing work and lower-frequency library administration. Design: `LLMS/Design/20260411-dashboard-primary-workflow.md`. Resolution: made the dashboard page-first, removed reusable-block creation and folder administration from the route, added a lightweight shared-content library entry point, and ordered pages draft-first then by recent updates.
- [done] [11/04/2026] Reduce cognitive load in reusable block management by moving low-frequency structural actions behind progressive disclosure. Priority: High. Impact: keeps routine shared-content editing visible while demoting folder administration and other lower-frequency library maintenance tasks that currently compete for attention on the dashboard. Design: `LLMS/Design/20260411-reusable-block-management-disclosure.md`. Resolution: removed inline shared-content management from the dashboard, kept only a summary link into the library, and left folder administration and reusable-block creation to the dedicated library/sidebar surfaces.
- [todo] [11/04/2026] Define a stronger CMS visual direction before major UI implementation continues. Priority: High. Impact: gives future dashboard, sidebar, page-editor, and reusable-block work a stable design target instead of continuing with scaffold-level styling decisions. Design: `LLMS/Design/20260411-cms-visual-direction.md`. Notes: larger feature; review and approve the visual-direction design before using it to drive broader UI implementation.
- [blocked] [11/04/2026] Audit dashboard and sidebar interactions for accessibility and reduced-motion behavior after the new CMS structure lands. Priority: Med. Impact: ensures the redesign works for keyboard, screen-reader, reduced-motion, and touch users before the new patterns spread further. Blocked: depends on the dashboard workflow and reusable-block-management redesigns above, because auditing the current structure now would create churn against UI flows that are already queued to change. Notes: once the redesign stabilizes, run a targeted audit across dialogs, context menus, focus order, touch targets, drag/drop alternatives, and motion defaults before closing the implementation cycle.
- [done] [11/04/2026] Standardize destructive and structural action patterns across the CMS dashboard and sidebar. Priority: Low. Impact: gives editors one consistent interaction model for creating folders and confirming destructive actions instead of mixing inline submits, custom modals, and browser prompt/confirm dialogs. Resolution: promoted from `LLMS/Suggestions.md` as a small change, added a shared CMS action modal, replaced sidebar browser prompt/confirm flows with modal-driven folder create and delete confirmations, moved dashboard page and folder deletes into the same modal pattern, and updated the related CMS documentation.
- [done] [11/04/2026] Replace the current repeated card-on-card dashboard composition with a more intentional hierarchy built from spacing, typography, and fewer containers. Priority: Low. Impact: makes the dashboard easier to scan and reduces the generic scaffolded admin feel without changing workflow or information architecture. Resolution: promoted from `LLMS/Suggestions.md` as a small change, flattened the CMS dashboard sections into lighter list-based surfaces, reduced nested container density around page and shared-section management, tightened heading and helper-copy hierarchy, and updated the CMS dashboard documentation.
- [done] [11/04/2026] Improve CMS dashboard and sidebar action feedback by anchoring success, error, and pending states closer to the action that triggered them. Priority: Low. Impact: reduces ambiguity when editors create, update, or delete pages, folders, and reusable blocks. Resolution: promoted from `LLMS/Suggestions.md` as a small change, replaced the shared dashboard banner with page-local and shared-section-local feedback, added a pending state for folder saves, moved sidebar block action feedback into the sidebar `Blocks` section, and updated the CMS dashboard/sidebar documentation.
- [done] [11/04/2026] Rewrite CMS dashboard copy and labels so they speak to editors and marketers instead of scaffold text. Priority: Low. Impact: improves trust and clarity on the landing surface without changing the underlying dashboard structure. Resolution: promoted from `LLMS/Suggestions.md`, rewrote the CMS dashboard headings, descriptions, button labels, and empty-state text in editor-facing language, added a short CMS dashboard documentation page, and updated the documentation index.
- [done] [10/04/2026] Add reusable-block sidebar context menus for folder creation and item deletion. Priority: Low. Impact: reduces dashboard round-trips by letting editors manage block folders and reusable blocks directly from the sidebar. Resolution: promoted from `LLMS/Suggestions.md`, added route-independent sidebar action handlers, wired right-click menus for block folders and reusable blocks in the CMS sidebar, refreshed the client stores after create/delete actions, and updated the CMS sidebar documentation.
- [done] [10/04/2026] Fix non-functional page draft indicator in the CMS sidebar. Priority: Low. Impact: editors can see which pages are unpublished or have unpublished draft changes directly in the page tree. Resolution: promoted from `LLMS/Suggestions.md`, added a shared page draft-status helper, rendered `Draft` badges for matching page tree items in the sidebar, added regression coverage for the helper logic, and updated the CMS sidebar documentation.
- [done] [10/04/2026] Add a draft/publish workflow for reusable blocks. Priority: Med. Impact: prevents draft reusable-block edits from changing live shared content immediately and aligns reusable blocks with the versioned page model. Design: `LLMS/Design/20260410-reusable-block-draft-publish.md`. Resolution: added `reusable_block_versions` plus reusable-block draft/published pointers and SQL helpers, backfilled existing reusable blocks into draft/published version rows, switched reusable-block saves to create new draft revisions, added a publish action and draft/live status UI in the reusable block editor and library/sidebar, and updated CMS documentation for the new workflow.
- [done] [10/04/2026] Let pages reference reusable blocks from the page content editor. Priority: High. Impact: connects the reusable block library to the page system so shared content can actually be reused across pages. Design: `LLMS/Design/20260410-page-reusable-block-references.md`. Resolution: extended page content to support top-level live reusable block references, updated the page editor to add/reorder/remove reusable references alongside inline blocks, validated referenced ids on the client and server, added page-content regression coverage, and added a reusable-block delete modal that lists affected pages and strips those references from draft pages when the block is deleted.
- [done] [10/04/2026] Add editing support for nested `blocks` fields inside the reusable block editor. Priority: Med. Impact: makes container-style reusable blocks such as `section` fully usable instead of leaving nested content read-only. Design: `LLMS/Design/20260410-reusable-block-nested-editing.md`. Resolution: added reusable-block-specific nested editor helpers, switched the `/blocks/[id]` editor to submit full validated block content JSON, enabled inline nested block editing with drag-and-drop plus Up/Down reordering, added helper regression tests, and updated reusable block CMS documentation.
- [done] [10/04/2026] Invalid reusable block content crash while loading CMS after stricter block validation. Priority: High. Impact: the CMS home/layout load can fail entirely if an existing reusable block stores optional fields in a now-invalid legacy shape such as an empty date string. Resolution: normalized legacy reusable block content during parse, enforced `block_type` consistency, stopped writing empty optional date fields from the reusable block editor, and added regression coverage for legacy blank optional values.
- [done] [10/04/2026] Add page content editing to the page edit screen using the existing `page_versions.content` block model and code registry. Priority: High. Impact: turns the current content model into an actual editor workflow and closes the biggest gap in the CMS. Design: `LLMS/Design/20260410-page-content-editor.md`. Resolution: replaced the placeholder with a recursive draft content editor, added inline primitive and nested block editing, supported drag-and-drop plus Up/Down reordering within a list, saved `page_versions.content` alongside title/slug/SEO updates, added editor helper tests, and updated the CMS edit-page documentation.
- [done] [09/04/2026] Add page slug editing with validation and safe hierarchy updates. Priority: Med. Impact: removes a current CMS limitation and keeps sidebar/page routing usable as the tree grows. Resolution: added shared slug normalization/validation helpers, enabled slug editing on the page edit screen for non-root pages, blocked unsafe root-slug changes, redirected the editor after slug changes, added slug helper tests, and updated the CMS edit-page documentation.
- [done] [09/04/2026] PGRST205: "Could not find the table 'public.block_folders' in the schema cache" after reusable block library rollout. Priority: High. Impact: reusable block sidebar and editor fail because the new reusable-block tables are missing from the active database schema. Resolution: the migration exists at `supabase/migrations/20260410000100_reusable_blocks.sql`; apply the latest Supabase migrations to the local database so `public.block_folders` and `public.reusable_blocks` are created.
- [done] [09/04/2026] Add a reusable block library so editors can create many instances from code-defined block types and organize them in folders under a dedicated CMS sidebar section. Priority: Med. Impact: turns the existing block registry into editor-usable content and lays the foundation for later attaching reusable blocks to pages. Design: `LLMS/Design/20260410-reusable-block-instances.md`. Resolution: added reusable block and folder storage plus server helpers, sidebar library rendering, create/manage flows on the CMS home page, a dedicated `/blocks/[id]` editor for primitive fields, reusable block/tree tests, and CMS documentation for the new library workflow.
- [done] [09/04/2026] Define the initial typed block content model and registry for `page_versions.content`, including primitive field types and nested blocks. Priority: Med. Impact: establishes the foundation for page content editing, reusable blocks, and future layouts. Design: `LLMS/Design/20260409-block-content-model.md`. Resolution: added shared block/content types, a seeded block registry, recursive page-content parse/serialize validation helpers, typed draft-version content reads/writes, unit tests, and CMS documentation for the content model.
- [done] [09/04/2026] Add page-level SEO metadata fields to the CMS edit page using versioned page metadata instead of `pages` columns. Priority: Med. Impact: editors can manage meta description and related SEO settings in a way that can later draft/publish with page content. Design: `LLMS/Design/20260409-page-seo-metadata.md`. Resolution: added typed `meta.seo` parsing/serialization helpers, wired the edit page to load/save draft SEO metadata from `page_versions`, and documented the new editor behavior.
- [done] [09/04/2026] Regression: Supabase migrations/seed files removed, causing `PGRST205` for `public.pages`. Priority: High. Impact: CMS page load and CRUD fail because the core table is never created. Resolution: restored the committed Supabase migrations and seed so `supabase db reset` can recreate the schema.
- [done] [09/04/2026] Sidebar should mirror URL hierarchy with indentation and expand/collapse groups. Priority: Med. Impact: page navigation becomes easier to scan as page depth grows. Resolution: implemented nested sidebar navigation rooted at Home with collapsible child groups.
- [done] [09/04/2026] PGRST205: "Could not find the table 'public.pages' in the schema cache" causes 500 responses. Priority: unknown. Impact: server error on page load; likely migrations not applied or wrong database/schema cache. Resolution: applied migrations via Supabase local setup; table exists.
- [done] [09/04/2026] Mobile menu button broken — All pages (shared components). Click does nothing; expected sidebar to open. No console errors reported. Priority: unknown. Blocked: need confirmation that client-side hydration is running and a repro environment to observe the click handler firing. Resolution: caused by missing `public.pages` table; resolved after migrations.
- [done] [09/04/2026] Rename LLMS-GUIDE.md and LLMS-INDEX.md files to AGENTS-GUIDE.md and AGENTS-INDEX.md files and ensure this is reflected in all md files in the project. Priority: Highest.
- [done] [09/04/2026] Create an initial edit page for pages.
- [done] [09/04/2026] Sidebar does not reflect page deletions until a full reload. Priority: unknown. Impact: stale navigation list; delete looks inconsistent.
- [done] [09/04/2026] Third-level sidebar items opened a 404 instead of the edit page. Priority: High. Impact: nested pages deeper than one segment could not be edited from the sidebar. Resolution: changed the edit route from `/edit/[slug]` to `/edit/[...slug]` and updated edit links to target nested slugs correctly.
